<% content_for :page_title do %>
    <%= Spree.t(:new_shipping_manifest) %>
<% end %>

<% content_for :page_actions do %>
    <li>
      <%= button_link_to Spree.t(:back_to_shipping_manifest), spree.admin_pws_shipping_manifests_path, :icon => 'arrow-left' %>
    </li>
<% end %>

<%= form_for [:admin, @shipping_manifest] do |f| %>

    <%= render :partial => 'form', :locals => { :f => f } %>

    <fieldset class="no-border-top">
      <br>
      <div class="filter-actions actions" data-hook="buttons">
        <%= button Spree.t(:create), 'ok' %>
      </div>
    </fieldset>

<% end %>
